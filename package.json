{
  "name": "cpm-enterprise-project-controls",
  "private": true,
  "version": "1.0.0-rc.4",
  "description": "Offline-first construction scheduling, estimating, controls, reporting, and recovery workbench.",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "audit:dependencies": "npm audit --audit-level=high",
    "sbom": "mkdir -p artifacts && npm sbom --sbom-format cyclonedx > artifacts/sbom.cdx.json",
    "check": "npm run test && npm run build",
    "qualify": "npm run check && npm run audit:dependencies && npm run sbom && npm run test:e2e"
  },
  "dependencies": {
    "dexie": "4.4.3",
    "react": "19.2.6",
    "react-dom": "19.2.6"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@playwright/test": "1.61.1",
    "@types/react": "19.2.14",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "6.0.2",
    "fake-indexeddb": "6.2.5",
    "typescript": "6.0.3",
    "vite": "8.1.5",
    "vite-plugin-pwa": "1.3.0",
    "vitest": "4.1.10"
  },
  "overrides": {
    "ejs": "6.0.1",
    "serialize-javascript": "7.0.7"
  }
}
